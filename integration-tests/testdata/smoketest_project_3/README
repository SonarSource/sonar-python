This test projects is used to test the feature "Importing Test Statistics"

The following scenario is provided:

3. ** importing test statistics considering details, some resources can be located **
   GIVEN a project
   WHEN we analyse this project considering details
   THEN the plugins manages to locate every test in nosetests_good but none in nosetests_bad
       AND the project-level measures are aggregations of values found in nosetests_good

4. ** importing test statistics considering details, some resources can be located **
   GIVEN this project
   WHEN we analyse this project with skipDetails=true
   THEN the measures in both nosetest_good and nosetests_bad are counted and available as aggregation on project level
