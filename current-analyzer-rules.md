# Current Analyzer Rules

Derived from `python-checks/src/main/java/org/sonar/python/checks/OpenSourceCheckList.java`.

Feasibility is rated against **safe automatic rewrite**, not against how easy it is for a human to fix manually. See `quickfix-feasibility-criteria.md`.

| Rule | Issue description | Issue title | Current quickfix status | Estimated feasibility | Short justification |
| --- | --- | --- | --- | --- | --- |
| **S100** | Rename method "Incorrect_Method_Name" to match the regular expression ^[a-z_][a-z0-9_]*$. | Method names should comply with a naming convention | ❓ unknown | 3/10 | Several compliant names are possible; the analyzer cannot know the best one. |
| **S101** | Rename class "MyClass_WithNotCompliantName1" to match the regular expression ^_?([A-Z_][a-zA-Z0-9]*\\|[a-z_][a-z0-9_]*)$. | Class names should comply with a naming convention | ❓ unknown | 3/10 | Several compliant names are possible; the analyzer cannot know the best one. |
| **S104** | File "tooManyLinesInFile-2.py" has 3 lines, which is greater than 2 authorized. Split it into smaller files. | Files should not have too many lines of code | ❓ unknown | 0/10 | Splitting a large file is a refactor, not an automatic rewrite. |
| **S107** | Lambda has 14 parameters, which is greater than the 13 authorized. | Functions, methods and lambdas should not have too many parameters | ❓ unknown | 0/10 | Reducing parameter count requires API redesign. |
| **S108** | Either remove or fill this block of code. | Nested blocks of code should not be left empty | ✅ exists | 7/10 | Adding a TODO comment is algorithmic, but it is only a placeholder fix. |
| **S112** | Replace this generic exception class with a more specific one. | "Exception" and "BaseException" should not be raised | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S113** | Add a new line at the end of this file "missingNewlineAtEndOfFile1.py". | Files should end with a newline | ❓ unknown | 10/10 | Adding a trailing newline is a deterministic one-line edit. |
| **S116** | Rename this field "myField" to match the regular expression ^[_a-z][a-z0-9_]+$. | Field names should comply with a naming convention | ❓ unknown | 3/10 | Several compliant names are possible; the analyzer cannot know the best one. |
| **S117** | Rename this parameter "inputPar2" to match the regular expression ^[_a-z][a-z0-9_]+$. | Local variable and function parameter names should comply with a naming convention | ❓ unknown | 3/10 | Several compliant names are possible; the analyzer cannot know the best one. |
| **S125** | Remove this commented out code. | Sections of code should not be commented out | ❓ unknown | 8/10 | Removing commented-out code is a direct local deletion. |
| **S134** | Refactor this code to not nest more than 4 "if", "for", "while", "try" and "with" statements. | Control flow statements "if", "for", "while", "try" and "with" should not be nested too deeply | ❓ unknown | 1/10 | Reducing nesting depth is a refactor, not a deterministic local rewrite. |
| **S138** | This function "function_101" has 101 lines of code, which is greater than the 100 authorized. Split it into smaller functions. | Functions should not have too many lines of code | ❓ unknown | 0/10 | Splitting a long function is a refactor, not a local edit. |
| **S139** | Move this trailing comment on the previous empty line. | Comments should not be located at the end of lines of code | ✅ exists | 9/10 | A local deterministic rewrite already exists in the analyzer. |
| **S905** | Remove or refactor this statement; it has no side effects. | Non-empty statements should change control flow or have at least one side-effect | ❓ unknown | 4/10 | Several plausible edits exist, so there is no single canonical rewrite. |
| **S930** | Add 1 missing arguments; 'foo' expects 2 positional arguments. | The number and name of arguments passed to a function should match its parameters | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S935** | Return a value of type `bool` here. | Functions and methods should only return expected values | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S1045** | Catch this exception only once; it is already handled by a previous except clause. | All "except" blocks should be able to catch exceptions | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S1066** | Merge this if statement with the enclosing one. | Mergeable "if" statements should be combined | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S1110** | Remove those redundant parentheses. | Redundant pairs of parentheses should be removed | ✅ exists | 9/10 | A local deterministic rewrite already exists in the analyzer. |
| **S1128** | Remove this unused import. | Unnecessary imports should be removed | ❓ unknown | 10/10 | Removing an unused import is a deterministic local deletion. |
| **S1131** | Remove the useless trailing whitespaces at the end of this line. | Lines should not end with trailing whitespaces | ✅ exists | 10/10 | Removing trailing whitespace is a deterministic local deletion. |
| **S1134** | Take the required action to fix the issue indicated by this "FIXME" comment. | Track uses of "FIXME" tags | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S1135** | Complete the task associated to this "TODO" comment. | Track uses of "TODO" tags | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S1142** | This function has 3 returns or yields, which is more than the 2 allowed. | Functions should not contain too many return statements | ❓ unknown | 1/10 | Reducing the number of returns requires refactoring control flow. |
| **S1143** | Remove this "continue" statement from this "finally" block. | Break, continue and return statements should not occur in "finally" blocks | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S1144** | Remove this unused class-private '__unused' method. | Unused class-private methods should be removed | ❓ unknown | 2/10 | The edit reaches beyond a small local rewrite and can affect callers or API shape. |
| **S1172** | Remove the unused function parameter "a". | Unused function parameters should be removed | ❓ unknown | 1/10 | Removing a parameter safely requires updating callers. |
| **S1186** | Add a nested comment explaining why this function is empty, or complete the implementation. | Functions and methods should not be empty | ✅ exists | 7/10 | Placeholder comments or stubs are easy to insert, but they are not semantic rewrites. |
| **S1192** | Define a constant instead of duplicating this literal "aa aa" 3 times. | String literals should not be duplicated | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S1226** | Introduce a new variable or use its initial value before reassigning 'p'. | Function parameters initial values should not be ignored | ❓ unknown | 4/10 | Several plausible edits exist, so there is no single canonical rewrite. |
| **S1244** | Do not perform equality checks with floating point values. | Floating point numbers should not be tested for equality | ✅ exists | 8/10 | The analyzer can rewrite to `isclose`, but it depends on local call context. |
| **S1309** | Is #noqa used to exclude false-positive or to hide real quality flaw? | Track uses of noqa comments | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S1313** | Make sure using this hardcoded IP address "1.2.3.4" is safe here. | IP addresses should not be hardcoded | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S1451** | Update the header of this file to match the expected one. | Track lack of copyright and license headers | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S1481** | Remove the unused local variable "unread_local". | Unused local variables should be removed | ✅ exists | 8/10 | The existing fix is local, but only safe in guarded assignment shapes. |
| **S1515** | Add a parameter to the parent lambda function and use variable "j" as its default value; The value of "j" might change at the next loop iteration. | Functions and lambdas should not reference variables defined in enclosing loops | ❓ unknown | 6/10 | A closure/default-argument rewrite is local, but it changes function shape. |
| **S1523** | Make sure that this dynamic injection or execution of code is safe. | Dynamically executing code is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S1542** | Rename function "Badly_Named_Function" to match the regular expression ^[a-z_][a-z0-9_]*$. | Function names should comply with a naming convention | ❓ unknown | 3/10 | Several compliant names are possible; the analyzer cannot know the best one. |
| **S1578** | Rename this module to match this regular expression: "(([a-z_][a-z0-9_]*)\\|([A-Z][a-zA-Z0-9]+))$". | Module names should comply with a naming convention | ❓ unknown | 3/10 | Several compliant names are possible; the analyzer cannot know the best one. |
| **S1607** | Provide a reason for skipping this test. | A reason should be provided when skipping a test | ❓ unknown | 1/10 | The analyzer cannot invent a meaningful skip reason. |
| **S1656** | Remove or correct this useless self-assignment. | Variables should not be self-assigned | ❓ unknown | 3/10 | The code is local, but the intended correction is not always unique. |
| **S1700** | Rename field "myClass" | A field should not duplicate the name of its containing class | ❓ unknown | 3/10 | Several valid replacement names exist; the analyzer cannot know the best one. |
| **S1707** | Add a citation of the person who can best explain this comment. | Track "TODO" and "FIXME" comments that do not contain a reference to a person | ❓ unknown | 1/10 | The analyzer cannot invent the right human reference for the comment. |
| **S1716** | Remove this "break" statement | "break" and "continue" should not be used outside a loop | ❓ unknown | 9/10 | Removing an invalid `break`/`continue` is a local deterministic edit. |
| **S1717** | Remove this "\", add another "\" to escape it, or make this a raw string. | "\" should only be used as an escape character outside of raw strings | ❓ unknown | 2/10 | There are multiple valid rewrites: escape it, make it raw, or remove it. |
| **S1720** | Add a docstring to this function. | Docstrings should be defined | ✅ exists | 7/10 | A docstring stub is easy to insert, but it is still placeholder content. |
| **S1721** | Remove the parentheses after this "assert" keyword. | Parentheses should not be used after certain keywords | ❓ unknown | 10/10 | Removing those keyword parentheses is a direct syntax-only edit. |
| **S1722** | Add inheritance from "object" or some other new-style class. | New-style classes should be used | ❓ unknown | 8/10 | Adding `object` inheritance is a local canonical compatibility fix. |
| **S1751** | Refactor this loop to do more than one iteration. | Loops with at most one iteration should be refactored | ❓ unknown | 1/10 | This is a refactor problem, not a deterministic rewrite. |
| **S1763** | Delete this unreachable code or refactor the code to make it reachable. | All code should be reachable | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S1764** | Correct one of the identical sub-expressions on both sides of operator "==". | Identical expressions should not be used on both sides of a binary operator | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S1845** | Rename method "GO_UP" to prevent any misunderstanding/clash with method "go_up" defined on line 2 | Methods and field names should not differ only by capitalization | ❓ unknown | 3/10 | The conflicting symbol should be renamed, but the best new name is not uniquely known. |
| **S1854** | Remove this assignment to local variable 'x'; the value is never used. | Unused assignments should be removed | ✅ exists | 9/10 | Removing an unused assignment is a local deterministic edit in supported shapes. |
| **S1862** | This branch duplicates the one on line 3. | Related "if/else if" statements should not have the same condition | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S1871** | Either merge this branch with the identical one on line "%s" or change one of the implementations. | Two branches in a conditional structure should not have exactly the same implementation | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S1940** | Use the opposite operator ("!=") instead. | Boolean checks should not be inverted | ✅ exists | 10/10 | This is a direct operator swap with a known target. |
| **S2053** | Make this salt unpredictable. | Password hashing functions should use an unpredictable salt | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S2068** | "password" detected here, review this potentially hard-coded credential. | Credentials should not be hard-coded | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S2077** | Make sure that formatting this SQL query is safe here. | Formatting SQL queries is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S2092** | Make sure creating this cookie without the "secure" flag is safe. | Cookies should have the "secure" flag | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S2115** | Add password protection to this database. | A secure password should be used when connecting to a database | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S2159** | Remove this equality check between incompatible types; it will always return False. | Unnecessary equality checks should not be made | ❓ unknown | 2/10 | The issue is local, but there is no single canonical rewrite beyond changing logic. |
| **S2190** | Add a way to break out of this function's recursion. | Recursion should not be infinite | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S2201** | The return value of "round" must be used. | Return values from functions without side effects should not be ignored | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S2208** | Import only needed names or import the module and then use its members. | Wildcard imports should not be used | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S2245** | Make sure that using this pseudorandom number generator is safe here. | Using pseudorandom number generators (PRNGs) is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S2257** | Make sure using a non-standard cryptographic algorithm is safe here. | Custom cryptographic algorithms should not be used | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S2275** | Use only positional or only named fields, don't mix them. | String formatting should not lead to runtime errors | ❓ unknown | 2/10 | The analyzer cannot know how the format string should be redesigned. |
| **S2325** | Make this method static. | Methods and properties that don't access instance data should be static | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S2612** | Make sure this permission is safe. | File permissions should not be set to world-accessible values | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S2638** | Remove parameter param2 or provide default value. | Method overrides should not change contracts | ❓ unknown | 1/10 | The rule itself offers multiple valid fixes and no single canonical one. |
| **S2710** | Rename "%s" to a valid class parameter name or add the missing class parameter. | The first argument to class methods should follow the naming convention | ✅ exists | 7/10 | The target name is mostly known, but the fix can be rename-or-insert rather than one edit. |
| **S2711** | Remove this use of "return". | "yield" and "return" should not be used outside functions | ❓ unknown | 9/10 | Removing an invalid top-level `return` is a direct local deletion. |
| **S2712** | Only yield or return values from this function, not both. | "return" with a value should not be used in a generator function | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S2733** | Add the missing argument. | "__exit__" should accept type, value, and traceback arguments | ❓ unknown | 8/10 | The missing `__exit__` parameters are known and can be inserted mechanically. |
| **S2734** | Remove this yield statement. | "__init__" should not return a value | ❓ unknown | 9/10 | Removing `yield` from `__init__` is a direct local deletion. |
| **S2737** | Add logic to this except clause or eliminate it and rethrow the exception automatically. | "except" clauses should do more than raise the same issue | ❓ unknown | 1/10 | The analyzer cannot invent the missing exception-handling logic. |
| **S2755** | Disable access to external entities in XML parsing. | XML parsers should not be vulnerable to XXE attacks | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S2757** | Was += meant instead? | Non-existent operators like "=+" should not be used | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S2761** | Use the "bool()" builtin function instead of calling "not" twice. | Doubled prefix operators "not" and "~" should not be used | ❓ unknown | 9/10 | The `not not x` to `bool(x)` rewrite is direct and local. |
| **S2772** | Remove this unneeded "pass". | "pass" should not be used needlessly | ✅ exists | 9/10 | A local deterministic rewrite already exists in the analyzer. |
| **S2823** | Replace this symbol with a string; "__all__" can only contain strings. | Only strings should be listed in "__all__" | ❓ unknown | 8/10 | The target string can be derived from the current symbol name. |
| **S2836** | Add a "break" statement or remove this "else" clause. | Loops without "break" should not have "else" clauses | ❓ unknown | 3/10 | There are two valid edits and no single canonical one. |
| **S2876** | Return an object complying with iterator protocol. | "__iter__" should return an iterator | ❓ unknown | 1/10 | The analyzer cannot invent the correct iterator object to return. |
| **S3329** | Use a dynamically-generated, random IV. | Cipher Block Chaining IVs should be unpredictable | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S3330** | Make sure creating this cookie without the "HttpOnly" flag is safe. | Creating cookies without the "HttpOnly" flag is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S3358** | Extract this nested conditional expression into an independent statement. | Conditional expressions should not be nested | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S3403** | Remove this "is" check; it will always be False. | Identity operators should not be used with dissimilar types | ❓ unknown | 8/10 | Replacing an always-false identity check with a constant is local and deterministic. |
| **S3457** | Add replacement fields or use a normal string instead of an f-string. | String formatting should be used correctly | ❓ unknown | 1/10 | The analyzer cannot decide whether to change the string template or the calling style. |
| **S3516** | Refactor this method to not always return the same value. | Functions returns should not be invariant | ❓ unknown | 1/10 | Changing invariant returns requires semantic refactoring. |
| **S3626** | Remove this redundant return. | Jump statements should not be redundant | ✅ exists | 10/10 | Removing a redundant jump is a direct local deletion. |
| **S3699** | Remove this use of the output from "append"; "append" doesn’t return anything. | The output of functions that don't return anything should not be used | ❓ unknown | 4/10 | The code is local, but the intended replacement depends on how the expression is used. |
| **S3752** | Explicitly specify the HTTP methods this endpoint accepts. | HTTP routes should restrict allowed HTTP methods | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S3776** | Refactor this function to reduce its Cognitive Complexity from 3 to the 0 allowed. | Cognitive Complexity of functions should not be too high | 🚫 infeasible | 0/10 | This is a refactor problem rather than a deterministic rewrite. |
| **S3801** | Refactor this function to use "return" consistently. | Functions should use "return" consistently | ❓ unknown | 1/10 | Making returns consistent requires control-flow refactoring, not one canonical edit. |
| **S3827** | condition_var is used before it is defined. Move the definition before. | Variables, classes and functions should be defined before being used | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S3862** | Replace this expression with an iterable object. | Iterable unpacking, "for-in" loops and "yield from" should use an Iterable object | ❓ unknown | 1/10 | The analyzer cannot know which iterable object should be used instead. |
| **S3923** | Remove this if statement or edit its code blocks so that they're not all the same. | All branches in a conditional structure should not have exactly the same implementation | ✅ exists | 6/10 | Some identical-branch cases can be collapsed automatically, but not all. |
| **S3981** | The length of a collection is always ">=0", so update this test to either "==0" or ">0". | Collection sizes and array length comparisons should make sense | ❓ unknown | 4/10 | Several plausible edits exist, so there is no single canonical rewrite. |
| **S3984** | Raise this exception or remove this useless statement. | Exceptions should not be created without being raised | ✅ exists | 7/10 | A local rewrite exists, but there can be more than one valid intent-preserving edit. |
| **S3985** | Remove this unused private '__unused_cls' class. | Unused private nested classes should be removed | ❓ unknown | 2/10 | The edit reaches beyond a small local rewrite and can affect callers or API shape. |
| **S4143** | Verify this is the key that was intended; a value has already been saved for it on line 3. | Collection content should not be replaced unconditionally | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S4144** | Update this function so that its implementation is not identical to method on line 2. | Functions and methods should not have identical implementations | ✅ exists | 5/10 | A helper rewrite exists in some cases, but duplicate implementations are not uniformly auto-fixable. |
| **S4423** | Change this code to use a stronger protocol. | Weak SSL/TLS protocols should not be used | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S4426** | Use a key length of at least 2048 bits. | Cryptographic key generation should be based on strong parameters | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S4433** | Provide a password when authenticating to this LDAP server. | LDAP connections should be authenticated | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S4487** | Remove this unread private attribute '__foo' or refactor the code to use its value. | Unread "private" attributes should be removed | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S4502** | Make sure disabling CSRF protection is safe here. | Disabling CSRF protections is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S4507** | Make sure this debug feature is deactivated before delivering the code in production. | Delivering code in production with debug features activated is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S4790** | Make sure that hashing data is safe here. | Using weak hashing algorithms is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S4828** | Make sure that sending signals is safe here. | OS process signal parameters should be validated | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S4830** | Enable server certificate validation on this SSL/TLS connection. | Server certificates should be verified during SSL/TLS connections | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5042** | Make sure that expanding this archive file is safe here. | Expanding archive files should not be done without controlling resource consumption | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5122** | Make sure this permissive CORS policy is safe here. | Cross-Origin Resource Sharing (CORS) policy should be restricted to trusted origins | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5247** | Make sure disabling auto-escaping feature is safe here. | Disabling auto-escaping in template engines is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5332** | Using http protocol is insecure. Use https instead | Using clear-text protocols is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5344** | Use strong scrypt parameters. | Passwords should not be stored in plaintext or with a fast hashing algorithm | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5361** | Replace this "re.sub()" call by a "str.replace()" function call. | `str.replace` should be preferred to `re.sub` | ❓ unknown | 9/10 | The `re.sub` to `str.replace` rewrite is local and direct. |
| **S5439** | Remove this configuration disabling autoescape globally. | HTML autoescape mechanism should not be globally disabled | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5443** | Make sure publicly writable directories are used safely here. | Temporary files should not be created in publicly writable directories | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5445** | 'os.tempnam' is insecure. Use 'tempfile.TemporaryFile' instead | Insecure temporary file creation methods should not be used | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5527** | Enable server hostname verification on this SSL/TLS connection. | Server hostnames should be verified during SSL/TLS connections | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5542** | Use secure mode and padding scheme. | Encryption algorithms should be used with secure mode and padding scheme | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5547** | Use a strong cipher algorithm. | Cipher algorithms should be robust | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5549** | Remove duplicate values for parameter "b" in "func" call. | Function arguments should be passed only once | ❓ unknown | 3/10 | Removing one duplicate argument is local, but keeping the right value is not always obvious. |
| **S5603** | Remove this unused function declaration. | Unused scope-limited definitions should be removed | ❓ unknown | 2/10 | The edit reaches beyond a small local rewrite and can affect callers or API shape. |
| **S5607** | Fix this invalid "<" operation between incompatible types (int and str). | Operators should be used on compatible types | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5632** | Change this code so that it raises an object deriving from BaseException. | Raised Exceptions must derive from BaseException | ❓ unknown | 1/10 | The analyzer cannot safely choose which exception type should be raised. |
| **S5642** | Change the type of 1337; type int does not support membership protocol. | "in" and "not in" operators should be used on objects supporting them | ❓ unknown | 1/10 | The analyzer cannot infer the right replacement object type. |
| **S5644** | Fix this code; this expression does not have a "__delitem__" method. | Item operations should be done on objects supporting them | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5655** | Change this argument; Function "function_with_keyword_only" expects a different type | Arguments given to functions should be of an expected type | ❓ unknown | 1/10 | Choosing the correct argument value/type requires domain knowledge. |
| **S5659** | Don't use a JWT token without verifying its signature. | JWT should be signed and verified | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5685** | Use an assignment statement ("=") instead; ":=" operator is confusing in this context. | Walrus operator should not make code confusing | ❓ unknown | 6/10 | The walrus rewrite is local, but it may require statement extraction. |
| **S5704** | Refactor this code so that any active exception raises naturally. | Bare "raise" statements should not be used in "finally" blocks | ❓ unknown | 1/10 | This requires exception-flow refactoring rather than a deterministic rewrite. |
| **S5706** | Remove this "raise" statement and return "False" instead. | Special method "__exit__" should not re-raise the provided exception | ❓ unknown | 10/10 | The rule defines one canonical replacement: return `False`. |
| **S5707** | Replace this expression with an exception or None | Exceptions' "__cause__" should be either an Exception or None | ❓ unknown | 2/10 | The analyzer cannot know which exception object should become the cause. |
| **S5708** | Change this expression to be a class deriving from BaseException or a tuple of such classes. | Caught Exceptions must derive from BaseException | ✅ exists | 5/10 | The issue is local, but making an exception derive from `Exception` changes class design. |
| **S5709** | Derive this class from "Exception" instead of "BaseException". | Custom Exception classes should inherit from "Exception" or one of its subclasses | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5712** | Return "NotImplemented" instead of raising "NotImplementedError" | Some special methods should return "NotImplemented" instead of raising "NotImplementedError" | ✅ exists | 10/10 | The rule defines one canonical replacement: return `NotImplemented`. |
| **S5713** | Remove this redundant Exception class; it derives from another which is already caught. | A subclass should not be in the same "except" statement as a parent class | ✅ exists | 10/10 | Removing the redundant caught exception is a local deterministic deletion. |
| **S5714** | Rewrite this "except" expression as a tuple of exception classes. | Boolean expressions of exceptions should not be used in "except" statements | ✅ exists | 9/10 | The tuple rewrite is local and mechanically derivable. |
| **S5717** | Change this default value to "None" and initialize this parameter inside the function/method. | Function parameters' default values should not be modified or assigned | ✅ exists | 8/10 | The `None`-default rewrite is local and canonical, but still guarded. |
| **S5719** | Add a "self" or class parameter | Instance and class methods should have at least one positional parameter | ✅ exists | 8/10 | Adding `self`/`cls` is a local signature fix with a known target. |
| **S5720** | Rename "some_argument" to "self" or add the missing "self" parameter. | "self" should be the first argument to instance methods | ❓ unknown | 8/10 | The missing/incorrect `self` name is locally derivable. |
| **S5722** | Remove 1 parameters. Method __mul__ should have 2 parameters. | Special methods should have an expected number of parameters | ❓ unknown | 6/10 | The signature fix is local, but it can mean inserting or deleting parameters depending on the case. |
| **S5724** | Remove 1 parameters; property getter methods receive only "self". | Property getter, setter and deleter methods should have the expected number of parameters | ❓ unknown | 6/10 | The signature fix is local, but it is not always the same edit for getters/setters/deleters. |
| **S5727** | Remove this identity check; it will always be True. | Comparison to None should not be constant | ❓ unknown | 8/10 | Replacing an always-constant `None` identity check is a local deterministic edit. |
| **S5747** | Remove this "raise" statement or move it inside an "except" block. | Bare "raise" statements should only be used in "except" blocks | ❓ unknown | 2/10 | There are multiple valid rewrites and no single canonical one. |
| **S5754** | Reraise this exception to stop the application as the user expects | "SystemExit" should be re-raised | ✅ exists | 8/10 | Re-raising `SystemExit` is a local canonical rewrite. |
| **S5756** | Fix this call; "myvar" has type MyNonCallable and it is not callable. | Calls should not be made to non-callable values | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5780** | Change or remove duplicates of this key. | Expressions creating dictionaries should not have duplicate keys | ❓ unknown | 2/10 | The duplicate dictionary entry can be changed or removed; the right intent is not unique. |
| **S5781** | Change or remove duplicates of this key. | Expressions creating sets should not have duplicate values | ❓ unknown | 2/10 | The duplicate set entry can be changed or removed; the right intent is not unique. |
| **S5795** | Replace this "is" operator with "=="; identity operator is not reliable here. | Identity comparisons should not be used with cached types | ✅ exists | 10/10 | This is a direct operator replacement. |
| **S5796** | Replace this "is" operator with "==". | New objects should not be created only to check their identity | ✅ exists | 10/10 | This is a direct operator replacement. |
| **S5797** | Replace this expression; used as a condition it will always be constant. | Constants should not be used as conditions | ❓ unknown | 4/10 | The condition is local, but simplifying control flow is not always one edit. |
| **S5799** | Merge these implicitly concatenated strings; or did you forget a comma? | Implicit string and byte concatenations should not be confusing | ✅ exists | 7/10 | The rewrite is local, but there are several valid explicit forms. |
| **S5806** | Rename this variable; it shadows a builtin. | Builtins should not be shadowed by local variables | ✅ exists | 6/10 | A rename is possible, but the replacement name is heuristic rather than uniquely correct. |
| **S5807** | Change or remove this string; "NeverDefined" is not defined. | Only defined names should be listed in "__all__" | ❓ unknown | 2/10 | Undefined names in `__all__` can be removed or replaced; the right intent is ambiguous. |
| **S5828** | Fix this invalid mode string. | The "open" builtin function should be called with a valid mode | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5842** | Rework this part of the regex to not match the empty string. | Repeated patterns in regular expressions should not match the empty string | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5843** | Simplify this regular expression to reduce its complexity from 21 to the 20 allowed. | Regular expressions should not be too complicated | ❓ unknown | 2/10 | A regex simplification may exist, but there is no single canonical rewrite. |
| **S5845** | Change this assertion to not compare dissimilar types (A and B). | Assertions comparing incompatible types should not be made | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5850** | Group parts of the regex together to make the intended operator precedence explicit. | Alternatives in regular expressions should be grouped when used with anchors | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5852** | Make sure the regex used here, which is vulnerable to exponential runtime due to backtracking, cannot lead to denial of service. | Using slow regular expressions is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S5855** | Remove or rework this redundant alternative. | Regex alternatives should not be redundant | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S5856** | Fix the syntax error inside this regex. | Regular expressions should be syntactically valid | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5857** | Replace this use of a reluctant quantifier with "[^>]+". | Character classes should be preferred over reluctant quantifiers in regular expressions | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S5860** | There is no group named 'b' in the regular expression. | Names of regular expressions named groups should be used | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5864** | Fix this call; Previous type checks suggest that "param1" has type int and it is not callable. | Type checks shouldn't be confusing | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5868** | Extract 1 Grapheme Cluster(s) from this character class. | Unicode Grapheme Clusters should be avoided inside regex character classes | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5869** | Remove duplicates in this character class. | Character classes in regular expressions should not contain the same character twice | ❓ unknown | 9/10 | The replacement is local and has one obvious target. |
| **S5886** | Return a value of type "int" instead of "str" or update function "my_int_nok" type hint. | Function return types should be consistent with their type hint | ❓ unknown | 2/10 | The analyzer cannot safely choose between changing code and changing the type hint. |
| **S5890** | Replace the type hint "int" with "Optional[int]" or don't assign "None" to "foo" | Values assigned to variables should match their type annotations | ❓ unknown | 2/10 | The rule itself presents more than one valid rewrite path. |
| **S5899** | Rename this method so that it starts with "test" or remove this unused helper. | Test methods should be discoverable | ✅ exists | 9/10 | A local deterministic rewrite already exists in the analyzer. |
| **S5905** | Fix this assertion on a tuple literal. | Assert should not be called on a tuple literal | ✅ exists | 9/10 | This is a small local AST rewrite with one obvious simplification. |
| **S5906** | Consider using "assertEqual" instead. | The most specific "unittest" assertion should be used | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5914** | Replace this expression; its boolean value is constant. | Assertions should not fail or succeed unconditionally | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S5915** | Don’t perform an assertion here; An exception is expected to be raised before its execution. | Assertions should not be made at the end of blocks expecting an exception | ✅ exists | 7/10 | The indentation rewrite is algorithmic, but only in constrained block shapes. |
| **S5918** | Skip this test explicitly. | Tests should be skipped explicitly | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S5953** | a is not defined. Change its name or define it before using it | Variables, classes and functions should be either defined or imported | ❓ unknown | 4/10 | Several plausible edits exist, so there is no single canonical rewrite. |
| **S5994** | Change this impossible to match sub-pattern that conflicts with the previous possessive quantifier. | Regex patterns following a possessive quantifier should not always fail | ❓ unknown | 3/10 | A regex rewrite is possible, but the exact safe target is not unique. |
| **S5996** | Remove or replace this boundary that will never match because it appears before mandatory input. | Regex boundaries should not be used in a way that can never be matched | ❓ unknown | 3/10 | A regex rewrite is possible, but there is no single canonical boundary replacement. |
| **S6001** | Fix this backreference - it refers to a capturing group that doesn't exist. | Back references in regular expressions should only refer to capturing groups that are matched before the reference | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6002** | Remove or fix this lookahead assertion that can never be true. | Regex lookahead assertions should not be contradictory | ❓ unknown | 3/10 | A regex rewrite is possible, but the exact lookahead correction is not unique. |
| **S6019** | Fix this reluctant quantifier that will only ever match 0 repetitions. | Reluctant quantifiers in regular expressions should be followed by an expression that can't match the empty string | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6035** | Replace this alternation with a character class. | Single-character alternations in regular expressions should be replaced with character classes | ❓ unknown | 9/10 | Replacing a single-char alternation with a character class is direct and local. |
| **S6243** | Initialize this AWS client outside the Lambda handler function. | Reusable resources should be initialized at construction time of Lambda functions | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6246** | Avoid synchronous calls to other lambdas | Lambdas should not invoke other lambdas synchronously | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6249** | No bucket policy enforces HTTPS-only access to this bucket. Make sure it is safe here. | Authorizing HTTP communications with S3 buckets is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6252** | Omitting the "versioned" argument disables S3 bucket versioning. Make sure it is safe here. | Disabling versioning of S3 buckets is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6262** | AWS region should not be set with a hardcoded String | AWS region should not be set with a hardcoded String | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6265** | Make sure granting %s access is safe here. | S3 buckets should not grant access to all users or authenticated users | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6270** | Make sure granting public access is safe here. | AWS resource-based policies should not grant public access | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6275** | Make sure that using unencrypted volumes is safe here. | EBS volumes should be encrypted | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6281** | Disabling public access block settings allows public ACL/policies to be set on this S3 bucket. | Allowing public ACLs or policies on a S3 bucket is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6302** | Make sure granting all privileges is safe here. | Policies should not grant all privileges | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6303** | Make sure that using unencrypted databases is safe here. | Amazon RDS resources should be encrypted at rest | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6304** | Make sure granting access to all resources is safe here. | Policies granting access to all resources of an account are security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6308** | Make sure that using unencrypted %s domains is safe here. | OpenSearch domains should have encryption at rest enabled | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6317** | This policy is vulnerable to the "Create Policy Version" privilege escalation vector. Remove permissions or restrict the set of resources they apply to. | AWS IAM policies should limit the scope of permissions given | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6319** | Omitting kms_key_id disables encryption of SageMaker notebook instances. Make sure it is safe here. | SageMaker notebook instances should be encrypted at rest | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6321** | Change this IP range to a subset of trusted IP addresses. | Administration services access should be restricted to specific IP addresses | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6323** | Remove this empty alternative. | Alternation in regular expressions should not contain empty alternatives | ❓ unknown | 9/10 | Removing an empty alternative is a direct local regex edit. |
| **S6326** | Replace spaces with quantifier `{3}`. | Regular expressions should not contain multiple spaces | ✅ exists | 10/10 | The regex replacement is exact and local. |
| **S6327** | Omitting "%s" disables SNS topics encryption. Make sure it is safe here. | Using unencrypted SNS topics is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6328** | Referencing non-existing group: 2. | Replacement strings should reference existing regular expression groups | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S6329** | Make sure allowing public network access is safe here. | Allowing public network access to cloud resources is security-sensitive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6330** | Setting "sqs_managed_sse_enabled" to "false" disables SQS queues encryption. Make sure it is safe here. | SQS queues should be encrypted | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6331** | Remove this empty group. | Regular expressions should not contain empty groups | ❓ unknown | 10/10 | Removing an empty regex group is an exact local edit. |
| **S6332** | Make sure that using unencrypted file systems is safe here. | Amazon EFS file systems should be encrypted | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6333** | Ensure this API route requires authentication. | AWS API Gateway should require authentication | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6353** | Use simple repetition 'x+' instead of 'xx*'. | Regular expression quantifiers and character classes should be used concisely | ✅ exists | 9/10 | A local deterministic rewrite already exists in the analyzer. |
| **S6377** | Change this code to only accept signatures computed from a trusted party. | XML signatures should be validated securely | ❓ unknown | 1/10 | Trusted-signature validation is a security decision, not a simple rewrite. |
| **S6395** | Unwrap this unnecessarily grouped subpattern. | Non-capturing groups without quantifier should not be used | ✅ exists | 10/10 | Unwrapping a useless non-capturing group is a deterministic local edit. |
| **S6396** | Remove this unnecessary quantifier. | Superfluous curly brace quantifiers should be avoided | ❓ unknown | 10/10 | Removing a superfluous quantifier is a deterministic local edit. |
| **S6397** | Replace this character class by the character itself. | Character classes in regular expressions should not contain only one character | ✅ exists | 10/10 | This is a direct regex text simplification. |
| **S6418** | "secret" detected here, make sure this is not a hard-coded secret. | Secrets should not be hard-coded | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S6437** | Revoke and change this password, as it is compromised. | Credentials should not be hard-coded | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S6463** | Omitting "allow_all_outbound" enables unrestricted outbound communications. Make sure it is safe here. | Security groups should not allow unrestricted outbound traffic | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6468** | Avoid catching ExceptionGroup exception with 'except*' | ExceptionGroup and BaseExceptionGroup should not be caught with except* | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6537** | Consider replacing this octal escape sequence with a Unicode or hexadecimal sequence instead. | Octal escape sequences should not be used in regular expressions | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6538** | Add a return type hint to this function declaration. | Function returns should have type hints | ✅ exists | 6/10 | A fix exists only for constrained inferred return types and `None` cases. |
| **S6540** | Add a type hint to this function parameter. | Function parameters should have type hints | ❓ unknown | 2/10 | The analyzer cannot safely invent the missing parameter type. |
| **S6542** | Use a more specific type than `Any` for this type hint. | Any should not be used as a type hint | ❓ unknown | 1/10 | The analyzer cannot choose the more specific replacement for `Any`. |
| **S6543** | Add a type argument to this generic type. | Type hints of generic types should specify their type parameters | ❓ unknown | 2/10 | The missing type parameter cannot be chosen safely without domain knowledge. |
| **S6545** | Use the built-in generic type `tuple` instead of its typing counterpart. | Built-in generic types should be preferred over the typing module in type hints | ✅ exists | 9/10 | The preferred built-in generic is known from the current type hint. |
| **S6546** | Use a union type expression for this type hint. | Union type expressions should be preferred over "typing.Union" in type hints | ❓ unknown | 9/10 | The `typing.Union` to `\|` rewrite is local and canonical. |
| **S6552** | Move this '@receiver' decorator to the top of the other decorators. | The '@receiver' (Django) and '@route' (Flask) decorators should be the outermost decorators | ✅ exists | 8/10 | Decorator reordering is local, but it still rewrites structure rather than a single token. |
| **S6553** | Replace this "null=True" flag with "blank=True". | 'null=True' should not be used on string-based fields in Django models | ✅ exists | 10/10 | The Django flag rewrite is a direct local substitution/removal. |
| **S6554** | Define a "__str__" method for this Django model. | Django models should define a "__str__" method | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6556** | Use an explicit context instead of passing "locals()" to this Django "render" call. | "locals()" should not be passed to a Django "render()" function | ❓ unknown | 2/10 | The analyzer cannot infer the right explicit context object to pass. |
| **S6559** | Set the fields of this form explicitly instead of using "exclude". | Fields of a Django ModelForm should be defined explicitly | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6560** | Use a dictionary object here, or set the "safe" flag to False. | The "safe" flag should be set to "False" when serializing non-dictionary objects in Django JSON-encoded responses. | ❓ unknown | 2/10 | The rule offers multiple valid rewrites and no single canonical one. |
| **S6659** | Use `startswith` here. | 'startswith' or 'endswith' methods should be used instead of string slicing in condition expressions | ❓ unknown | 9/10 | Replacing slicing checks with `startswith`/`endswith` is a direct local rewrite. |
| **S6660** | Use the `isinstance()` function here. | "isinstance()" should be preferred to direct type comparisons | ❓ unknown | 9/10 | The `type(...)` check to `isinstance(...)` rewrite is local and canonical. |
| **S6661** | Define function instead of this lambda assignment statement. | Assignments of lambdas to variables should be replaced by function definitions | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S6662** | Make sure this expression is hashable. | Set members and dictionary keys should be hashable | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6663** | Make sure this object defines an `__index__` method. | Sequence indexes must have an __index__ method | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6709** | Provide a seed for this random generator. | Results that depend on random number generation should be reproducible | ❓ unknown | 1/10 | The analyzer cannot safely choose the right seed value. |
| **S6711** | Use a "numpy.random.Generator" here instead of this legacy function. | numpy.random.Generator should be preferred to numpy.random.RandomState | ❓ unknown | 4/10 | The API replacement is local, but migration details are not always mechanical. |
| **S6714** | Pass a list to "np.array" instead of passing a generator. | Passing a list to np.array should be preferred over passing a generator | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6725** | Don't perform an equality/inequality check against "numpy.nan". | Equality checks should not be made against "numpy.nan" | ✅ exists | 8/10 | The `numpy.isnan` rewrite is local and canonical, but context-sensitive. |
| **S6727** | Provide the "abs_tol" parameter when using "math.isclose" to compare a value to 0. | The abs_tol parameter should be provided when using math.isclose to compare values to 0 | ✅ exists | 8/10 | Adding `abs_tol` is a local rewrite with a known default used by the implementation. |
| **S6729** | Use "np.nonzero" when only the condition parameter is provided to "np.where". | np.nonzero should be preferred over np.where when only the condition parameter is set | ✅ exists | 9/10 | The `np.where(cond)` to `np.nonzero(cond)` rewrite is direct and local. |
| **S6730** | Replace this deprecated "numpy" type alias with the builtin type "int". | Deprecated NumPy aliases of built-in types should not be used | ✅ exists | 10/10 | This is an exact alias replacement with a known target type. |
| **S6734** | Do not use "inplace=True" when modifying a dataframe. | inplace=True should not be used when modifying a Pandas DataFrame | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S6735** | Specify the "how", "on" and "validate" parameters of this merge. | When using pandas.merge or pandas.join, the parameters on, how and validate should be provided | ✅ exists | 6/10 | Defaults can be inserted mechanically, but the chosen values are placeholders. |
| **S6740** | Provide the "dtype" parameter when calling "pandas.read_csv". | 'dtype' parameter should be provided when using 'pandas.read_csv' or 'pandas.read_table' | ❓ unknown | 2/10 | The analyzer cannot safely choose the `dtype` mapping. |
| **S6741** | Do not use "DataFrame.values". | The "pandas.DataFrame.to_numpy()" method should be preferred to the "pandas.DataFrame.values" attribute | ✅ exists | 9/10 | The replacement API is known and local. |
| **S6742** | Refactor this long chain of instructions with "pandas.pipe" | pandas.pipe method should be preferred over long chains of instructions | ❓ unknown | 1/10 | Reworking a long pipeline into `pandas.pipe` is a refactor, not a local rewrite. |
| **S6779** | Don't disclose "Flask" secret keys. | Flask secret keys should not be disclosed | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6781** | Don't disclose "Flask" JWT secret keys. | JWT secret keys should not be disclosed | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6785** | Change this code to limit the depth of GraphQL queries. | GraphQL queries should not be vulnerable to Denial of Service attacks | ❓ unknown | 1/10 | Limiting query depth is a policy/configuration decision. |
| **S6786** | Disable introspection on this "GraphQL" server endpoint. | GraphQL introspection should be disabled in production | ❓ unknown | 1/10 | Disabling introspection is an environment/policy decision, not a local rewrite. |
| **S6792** | Use the "type" parameter syntax to declare this generic class. | Generic classes should be defined using the type parameter syntax | ❓ unknown | 7/10 | The new type-parameter syntax is locally rewritable, but it is more than a token swap. |
| **S6794** | Use a "type" statement instead of this "TypeAlias". | Type aliases should be declared with a "type" statement | ❓ unknown | 7/10 | The `TypeAlias` to `type` rewrite is local, but it still reshapes syntax. |
| **S6795** | Use a generic type parameter instead of a "TypeVar" in this type statement. | Generic type statements should not use "TypeVars" | ❓ unknown | 7/10 | The rewrite is local, but migrating from `TypeVar` to new syntax is structural. |
| **S6796** | Use a generic type parameter for this function instead of a "TypeVar". | Generic functions should be defined using the type parameter syntax | ❓ unknown | 7/10 | The rewrite is local, but migrating from `TypeVar` to new syntax is structural. |
| **S6799** | Do not nest f-strings too deeply. | "f-strings" should not be nested too deeply | ❓ unknown | 1/10 | Flattening nested f-strings requires restructuring the expression, not a canonical rewrite. |
| **S6863** | Specify an explicit HTTP status code for this error handler. | Flask error handlers should set HTTP status code | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S6882** | Provide a correct value for the `day` parameter. | Constructor attributes of date and time objects should be in the range of possible values | ❓ unknown | 1/10 | The analyzer cannot know the correct date component value. |
| **S6883** | Use %I (12-hour clock) or %H (24-hour clock) without %p (AM/PM). | The 12-hour format should be used with the AM/PM marker, otherwise the 24-hour format should be used | ❓ unknown | 2/10 | The format-string correction is not unique without intended time semantics. |
| **S6887** | Don't pass a "pytz.timezone" to the "datetime.datetime" constructor. | "pytz.timezone" should not be passed to the "datetime.datetime" constructor | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6890** | Don't use `pytz` module with Python 3.9 and later. | "zoneinfo" should be preferred to "pytz" when using Python 3.9 and later | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S6894** | Remove this `yearfirst=True` parameter or make sure the provided date(s) can be parsed accordingly. | Dates should be formatted correctly when using "pandas.to_datetime" with "dayfirst" or "yearfirst" arguments | ❓ unknown | 2/10 | The rule itself presents more than one valid rewrite path. |
| **S6900** | String must be either 7 characters long and contain only 0 and 1, or contain abbreviated weekdays. | Numpy weekmask should have a valid value | ❓ unknown | 4/10 | Several plausible edits exist, so there is no single canonical rewrite. |
| **S6903** | Don't use `datetime.datetime.utcnow` to create this datetime object. | Using timezone-aware "datetime" objects should be preferred over using "datetime.datetime.utcnow" and "datetime.datetime.utcfromtimestamp" | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S6908** | Make sure to avoid recursive calls in this function. | "tensorflow.function" should not be recursive | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6911** | Make sure this function does not depend on a global or free variable. | "tf.function" should not depend on global or free Python variables | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6918** | Refactor this variable declaration to be a singleton. | "tf.Variable" objects should be singletons when created inside of a "tf.function" | ❓ unknown | 1/10 | Making the variable a singleton requires redesigning object lifetime, not a local edit. |
| **S6919** | Remove this `input_shape` argument, it is deprecated. | The "input_shape" parameter should not be specified for "tf.keras.Model" subclasses | ❓ unknown | 10/10 | Removing the deprecated `input_shape` argument is a direct local deletion. |
| **S6925** | Don't set the `validate_indices` argument, it is deprecated. | The "validate_indices" argument should not be set for "tf.gather" function call | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S6928** | Make sure this Tensorflow function doesn't have Python side effects. | Python side effects should not be used inside a "tf.function" | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S6929** | Provide a value for the axis argument. | The reduction axis/dimension should be specified when using reduction operations | ✅ exists | 5/10 | An axis argument can be inserted mechanically, but the chosen value is generic. |
| **S6965** | Specify the HTTP methods this route should accept. | REST API actions should be annotated with an HTTP verb attribute | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S6969** | Specify a memory argument for the pipeline. | "memory" parameter should be specified for Scikit-Learn Pipeline | ✅ exists | 6/10 | The implementation inserts `memory=None`, which is mechanical but placeholder-like. |
| **S6971** | Avoid accessing transformers in a cached pipeline. | Transformers should not be accessed directly when a Scikit-Learn Pipeline uses caching | ✅ exists | 8/10 | The `named_steps` rewrite is local and based on nearby pipeline structure. |
| **S6972** | Provide valid parameters to the estimator. | Nested estimator parameters modification in a Pipeline should refer to valid parameters | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S6973** | Add the missing hyperparameter learning_rate for this Scikit-learn estimator. | Important hyperparameters should be specified for machine learning libraries' estimators and optimizers | ❓ unknown | 1/10 | The analyzer cannot safely choose a model hyperparameter value. |
| **S6974** | Move this estimated attribute in the `fit` method. | Subclasses of Scikit-Learn's "BaseEstimator" should not set attributes ending with "_" in the "__init__" method | ✅ exists | 7/10 | The fix is local but only safe for constrained estimator patterns. |
| **S6978** | Add a call to super().__init__(). | Subclasses of "torch.nn.Module" should call the initializer | ✅ exists | 9/10 | Adding `super().__init__()` is a known local rewrite. |
| **S6979** | Replace this call with a call to "torch.tensor". | "torch.tensor" should be used instead of "torch.autograd.Variable" | ❓ unknown | 9/10 | The replacement call target is known. |
| **S6982** | Set the module in training or evaluation mode. | "model.eval()" or "model.train()" should be called after loading the state of a PyTorch model | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S6983** | Specify the `num_workers` parameter. | The "num_workers" parameter should be specified for "torch.utils.data.DataLoader" | ❓ unknown | 2/10 | The analyzer cannot responsibly choose a `num_workers` value. |
| **S6984** | Provide a valid einops pattern. | Einops pattern should be valid | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S6985** | Replace this call with a safe alternative. | Models should not be loaded with "torch.load" without restricting deserialization | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S7483** | Remove this "timeout" parameter and use a timeout context manager instead. | Asynchronous functions should not accept timeout parameters | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S7484** | Refactor this loop to use an `Event` instead of polling with `sleep`. | Events should be used instead of `sleep` in asynchronous loops | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S7486** | Replace this call with "anyio.sleep_forever()" as the sleep duration exceeds 24 hours. | Long sleep durations should use sleep_forever() instead of arbitrary intervals | ✅ exists | 9/10 | Replacing long sleeps with `sleep_forever()` is a direct local rewrite. |
| **S7487** | Use an async subprocess call in this async function instead of a synchronous one. | Async functions should not contain synchronous subprocess calls | ❓ unknown | 5/10 | The change is local, but the async subprocess API choice is library-specific. |
| **S7488** | Replace this call to time.sleep() with an asynchronous sleep function. | Use non-blocking sleep functions in asynchronous code | ✅ exists | 8/10 | Non-blocking sleep replacements are local, but depend on async library context. |
| **S7489** | Use a thread executor to wrap blocking OS calls in this async function. | Async functions should not contain synchronous OS calls | ✅ exists | 7/10 | Wrapping a blocking OS call in a thread helper is local but library-specific. |
| **S7490** | Add a checkpoint inside this cancellation scope. | Cancellation scopes should contain checkpoints | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S7491** | Use trio.lowlevel.checkpoint() instead of trio.sleep. | Checkpoints should be used instead of sleep(0) | ✅ exists | 9/10 | The checkpoint replacement is explicit and local. |
| **S7492** | Unpack this comprehension expression | List comprehensions should not be used with "any()" or "all()" | ❓ unknown | 8/10 | Unpacking the comprehension is a local mechanical rewrite. |
| **S7493** | Use an asynchronous file API instead of synchronous open() in this async function. | Async functions should not contain synchronous file operations | ❓ unknown | 4/10 | The issue is local, but choosing the right async file API is not fully mechanical. |
| **S7494** | Replace list constructor call with a list comprehension. | Comprehensions should be used instead of constructors around generator expressions | ❓ unknown | 8/10 | A constructor around a generator can be rewritten locally. |
| **S7496** | Remove the redundant tuple constructor call. | Creation of collections with literals or comprehensions should not be wrapped in type constructors | ❓ unknown | 10/10 | Removing the redundant tuple constructor is a direct local simplification. |
| **S7497** | Ensure that the asyncio.CancelledError exception is re-raised after your cleanup code. | Cancellation exceptions should be re-raised after cleanup | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7498** | Replace this constructor call with a literal. | Literal syntax should be preferred when creating empty collections or dictionaries with keyword arguments | ✅ exists | 9/10 | Literal replacement is direct and local. |
| **S7499** | Use an async HTTP client in this async function instead of a synchronous one. | Async functions should not contain synchronous HTTP client calls | ❓ unknown | 4/10 | The issue is local, but choosing the right async HTTP client rewrite is not canonical. |
| **S7500** | Replace this comprehension with passing the iterable to the collection constructor call | Comprehensions only used to copy should be replaced with the respective constructor calls | ✅ exists | 9/10 | The collection-constructor rewrite is local and canonical. |
| **S7501** | Wrap this call to input() with the appropriate function from the asynchronous library. | Async functions should not contain input() calls | ✅ exists | 7/10 | Wrapping `input()` with an async helper is local but library-specific. |
| **S7502** | Save this task in a variable to prevent premature garbage collection. | Asyncio tasks should be saved to prevent premature garbage collection | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7503** | Use asynchronous features in this function or remove the `async` keyword. | Async functions should use async features | ❓ unknown | 1/10 | The rule itself offers multiple non-equivalent fixes. |
| **S7504** | Remove this unnecessary `list()` call on an already iterable object. | When iterating over an iterable object, using "list()" should be avoided | ✅ exists | 9/10 | Removing the unnecessary `list()` call is a direct local simplification. |
| **S7505** | Replace this map call with a comprehension. | Generators and comprehensions should be preferred over the usage of "map" and "lambda" when creating collection | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S7506** | Don't use a static key in a dictionary comprehension. | Dictionary comprehension should not use a static key | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S7507** | Replace this keyword argument with a positional argument at the first place. | "defaultdict" should not be initialized with "default_factory" as a keyword argument | ❓ unknown | 10/10 | This is a positional-vs-keyword rewrite with one exact target location. |
| **S7508** | Remove this redundant call. | Redundant collection functions should be avoided | ✅ exists | 9/10 | Removing a redundant collection wrapper is a local deterministic edit. |
| **S7510** | Remove this redundant reversed call, use reverse argument of the sorted function call instead | The "sorted" function call should not be passed to the "reversed" function as an argument | ❓ unknown | 9/10 | Rewriting `reversed(sorted(...))` is a direct local simplification. |
| **S7511** | Remove this redundant subscript reversal | Passing a reversed iterable to "set()", "sorted()", or "reversed()" should be avoided | ❓ unknown | 8/10 | Removing the redundant reversal is a local simplification with a known target. |
| **S7512** | Modify this loop to iterate over the dictionary's values. | Using ".items()" to iterate over a dictionary should be avoided if possible. | ❓ unknown | 8/10 | Switching from `.items()` to `.values()` is local and mechanically derivable. |
| **S7513** | Replace the TaskGroup with a direct function call when it only ever spawns one task. | TaskGroup/Nursery should not be used for a single start call | ❓ unknown | 6/10 | The replacement is local, but it still restructures task-group control flow. |
| **S7514** | Refactor the block to eliminate this "return" statement. | Control flow statements should not be used inside TaskGroup or Nursery blocks | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7515** | Use "async with" instead of "with" for this asynchronous context manager. | "async with" should be used for asynchronous resource management | ❓ unknown | 9/10 | Changing `with` to `async with` is a local syntactic rewrite. |
| **S7516** | Remove either the call to set or sorted. | "sorted" should not be wrapped directly inside "set" | ❓ unknown | 4/10 | There are at least two plausible rewrites and no single canonical one. |
| **S7517** | Use items to iterate over key-value pairs | Iteration over a dictionary key value pairs should be done with the items() method call | ✅ exists | 9/10 | The `.items()` rewrite is local and canonical. |
| **S7519** | Replace with dict fromkeys method call | Populating a dictionary with a constant value should be done with dict.fromkeys() method call | ❓ unknown | 9/10 | The `dict.fromkeys()` rewrite is direct and local when the value is constant. |
| **S7608** | Add the 'ExpectedBucketOwner' parameter to verify S3 bucket ownership. | S3 operations should verify bucket ownership using ExpectedBucketOwner parameter | ❓ unknown | 2/10 | The analyzer cannot know the correct bucket-owner value to inject. |
| **S7609** | Do not use AWS reserved namespace that begins with 'AWS/' for custom metrics. | AWS CloudWatch metrics namespace should not begin with `AWS/` | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S7613** | Fix the return value to be JSON serializable. | AWS Lambda handlers should return only JSON serializable values | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S7614** | Remove the `async` keyword from this AWS Lambda handler definition. | AWS Lambda handlers must not be an async function | ❓ unknown | 10/10 | Removing the unnecessary `async` keyword is a direct local edit. |
| **S7617** | Do not override reserved environment variable names in Lambda functions. | Reserved environment variable names should not be overridden in Lambda functions | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7618** | Set an explicit timeout for this network call to prevent hanging executions in Lambda functions. | Network calls in AWS Lambda functions shouldn't be made without explicit timeout parameters | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S7619** | Wrap this AWS client call in a try-except block to handle "botocore.exceptions.ClientError". | "botocore.exceptions.ClientError" should be explicitly caught and handled | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S7620** | Clean up this temporary file before the Lambda function completes. | AWS Lambda handlers should clean up temporary files in /tmp directory | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7621** | Use AWS waiters instead of custom polling loops. | AWS waiters should be used instead of custom polling loops | 🚫 infeasible | 1/10 | The rule still needs human judgment rather than one canonical edit. |
| **S7622** | Use a paginator to retrieve all results from this boto3 operation. | boto3 operations that support pagination should be performed using paginators or manual pagination handling | ❓ unknown | 3/10 | Pagination can be introduced, but the exact rewrite depends on the surrounding code flow. |
| **S7625** | Make sure using long-term access keys is safe here. | Long-term AWS access keys should not be used directly in code | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S7632** | Fix the syntax of this issue suppression comment. | Issue suppression comment should have the correct format | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7931** | NotImplemented should not be used in boolean contexts. | "NotImplemented" should not be used in boolean contexts | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7941** | Compression modules should be imported from the compression namespace. | Compression modules should be imported from the compression namespace | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7942** | This template string should be processed before use. | Template strings should be processed before use | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7943** | Template strings should not be concatenated with regular strings. | Template and str should not be concatenated directly | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S7945** | Use structural pattern matching (match/case) instead of isinstance() checks for template string processing. | Template string processing should use structural pattern matching | ❓ unknown | 1/10 | This is a refactor to pattern matching, not a deterministic local rewrite. |
| **S8370** | Do not use query parameters with POST requests; use path parameters or request body instead. | Query parameters should not be used in Flask POST requests | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S8371** | Use ".get()" method to safely access this header. | HTTP headers should be accessed safely to avoid KeyError exceptions | ❓ unknown | 8/10 | Replacing direct header access with `.get()` is a direct local rewrite. |
| **S8374** | Move this decorator to the "decorators" class attribute. | Flask class-based view decorators should be applied using the "decorators" attribute | ❓ unknown | 6/10 | Moving the decorator is local, but it changes class structure rather than a single token. |
| **S8375** | Handle the return value of "preprocess_request()" to ensure before-request handlers' responses are not ignored. | Flask "preprocess_request()" return values should be handled | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S8385** | Provide "mimetype" or "download_name" when calling "send_file" with a file-like object. | "send_file" should specify "mimetype" or "download_name" when used with file-like objects | ❓ unknown | 2/10 | The analyzer cannot know which metadata argument is correct to add. |
| **S8389** | Use "Form()" instead of "Body()" when handling file uploads; "Body()" expects JSON, which is incompatible with multipart/form-data. | FastAPI file upload endpoints should use "Form()" with Pydantic validators instead of "Body()" or "Depends()" | ❓ unknown | 8/10 | The `Body()` to `Form()` rewrite is direct and local. |
| **S8392** | Avoid binding the application to all network interfaces. | Web servers should not bind to all network interfaces | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S8396** | Add an explicit default value to this optional field. | Optional Pydantic fields should have explicit default values | ❓ unknown | 2/10 | The analyzer cannot safely invent the explicit default value. |
| **S8397** | Pass the application as an import string when using 'reload'. | FastAPI applications should be passed as import strings when using reload, debug, or workers | ❓ unknown | 1/10 | This is mainly a security/policy decision, not a safe automatic rewrite. |
| **S8400** | Return an empty body for this endpoint returning 204 status. | Endpoints returning 204 status should have an empty response body | ❓ unknown | 4/10 | The fix is local, but there is more than one way to make the body empty. |
| **S8401** | Include child routers before registering the parent router. | Child routers should be included before parent router registration | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S8405** | Use "content" parameter instead of "data" for bytes or text. | TestClient requests should use "content" parameter for bytes or text | ❓ unknown | 9/10 | Switching from `data` to `content` is a direct local API rewrite. |
| **S8409** | Remove this redundant "response_model" parameter; it duplicates the return type annotation. | FastAPI routes should not specify redundant "response_model" parameters | ❓ unknown | 10/10 | Removing the redundant parameter is a direct local deletion. |
| **S8410** | Use "Annotated" type hints for FastAPI dependency injection | FastAPI dependencies should use "Annotated" type hints | ❓ unknown | 7/10 | The `Annotated` rewrite is local, but it is more structural than a token swap. |
| **S8411** | Add path parameter "item_id" to the function signature. | FastAPI path parameters should be included in route function signatures | ❓ unknown | 7/10 | The missing path-parameter name is known from the route and can be inserted. |
| **S8412** | Replace this generic "route()" decorator with a specific HTTP method decorator. | Generic route decorators should not be used | ✅ exists | 9/10 | The specific HTTP method decorator is locally derivable. |
| **S8413** | Define the prefix in the "APIRouter" constructor instead of in "include_router()". | Router prefixes should be defined during "APIRouter" initialization | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S8414** | Add CORSMiddleware last in the middleware chain. | CORSMiddleware should be added last in the middleware chain | ❓ unknown | 6/10 | Middleware reordering is local, but it changes structure rather than a simple token. |
| **S8415** | Document this HTTPException with status code 404 in the "responses" parameter. | HTTPException responses should be documented in endpoint metadata | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S8490** | Remove this "@dataclass" decorator; it is incompatible with Enum classes. | Enum classes should not be decorated with "@dataclass" | ❓ unknown | 10/10 | Removing the incompatible `@dataclass` decorator is a direct local deletion. |
| **S8492** | Use "discard()" instead of checking membership before calling "remove()". | "set.discard()" should be used instead of checking membership before removal | ✅ exists | 9/10 | Replacing the guarded remove pattern with `discard()` is canonical. |
| **S8493** | Replace this "raise StopIteration" with a "return" statement. | "StopIteration" should not be raised inside generators | ❓ unknown | 9/10 | Replacing `raise StopIteration` with `return` is a direct local rewrite. |
| **S8494** | Add "age" to the class's "__slots__". | Attributes should only be assigned if they are declared in "__slots__" | ❓ unknown | 8/10 | The missing `__slots__` entry is explicitly known from the assigned attribute. |
| **S8495** | Refactor this function to always return tuples of the same length. | Functions should return tuples of consistent length | ❓ unknown | 1/10 | Harmonizing tuple shapes requires semantic refactoring, not a deterministic rewrite. |
| **S8500** | Add the missing comparison methods or use "functools.total_ordering". | Comparison methods should be defined completely | ❓ unknown | 1/10 | The rule itself suggests different non-equivalent fixes. |
| **S8502** | Use "set.update()" instead of a for-loop with "add()". | "set.update()" should be used instead of a for-loop with "add()" | ✅ exists | 8/10 | The `set.update()` rewrite is local, but only in constrained loop shapes. |
| **S8503** | Remove this membership test on an empty collection; it will always be the same value. | Membership tests should not use empty collections | ❓ unknown | 9/10 | Replacing an empty-collection membership test with a constant is direct and local. |
| **S8504** | Add a return statement to this property method. | Property methods should have a return statement | ❓ unknown | 1/10 | The analyzer cannot know what expression the property should return. |
| **S8505** | Use "@singledispatchmethod" instead of "@singledispatch" on methods defined in a class body. | "@singledispatch" and "@singledispatchmethod" should not be confused | ❓ unknown | 10/10 | The decorator replacement is direct and local. |
| **S8507** | Rename this string to match the variable name "MyType". | TypeVar, ParamSpec, and NewType names should match their assigned variable names | ❓ unknown | 9/10 | The target string value is already known from the variable name. |
| **S8508** | Replace this mutable value with an immutable default to avoid shared state. | Mutable default values should not be used with "dict.fromkeys()" or "ContextVar()" | ❓ unknown | 2/10 | The analyzer cannot safely invent the immutable replacement value. |
| **S8509** | Remove this duplicate base class. | Classes should not inherit from the same base class multiple times | ❓ unknown | 10/10 | Removing the duplicate base class is a direct local deletion. |
| **S8510** | Rename this loop variable; it shadows the outer loop variable "i". | Loop variables should not be reused in nested loops | ❓ unknown | 3/10 | The loop variable should be renamed, but the best new name is not uniquely known. |
| **S8511** | Reorder or remove base classes to fix this MRO conflict. | Multiple inheritance should not create Method Resolution Order (MRO) conflicts | ❓ unknown | 6/10 | The issue is local, but the exact safe rewrite is not always unique. |
| **S8512** | Remove this assignment; "x" is assigned again on line 4. | Class fields should not be defined multiple times | ❓ unknown | 8/10 | Removing the overwritten assignment is a local deterministic deletion. |
| **S8513** | Replace chained "startswith" calls with a single call using a tuple argument. | "startswith" and "endswith" calls should use tuples instead of multiple "or" operators | ✅ exists | 9/10 | Combining chained `startswith`/`endswith` calls into a tuple call is canonical. |
| **S8514** | Add a type annotation to this dataclass attribute. | Dataclass attributes should use type annotations | ❓ unknown | 2/10 | The missing type annotation cannot be chosen safely. |
| **S8515** | Remove either "covariant=True" or "contravariant=True"; a TypeVar cannot be both covariant and contravariant. | TypeVars should not be both covariant and contravariant | ❓ unknown | 3/10 | One flag must be removed, but the analyzer cannot know which intent is correct. |
| **S8516** | Consume this group iterator inside the loop, or materialize it into a collection. | Group iterators from "itertools.groupby" should not be reused | ✅ exists | 6/10 | Materializing the iterator is easy, but it changes performance/consumption behavior. |
| **S8517** | Use "min()" instead of sorting to find this value. | "sorted()" should not be used with indexing to find minimum or maximum values | ❓ unknown | 9/10 | Replacing sort-and-index with `min()`/`max()` is a known local rewrite. |
| **S8518** | Unpack the value from 'enumerate()' directly instead of using an index lookup. | "enumerate()" values should be unpacked instead of using index lookups | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S8519** | Replace "list(...)[0]" with "next(iter(...))" to avoid materializing the entire iterable. | "list(...)[0]" should not be used to get the first element | ✅ exists | 9/10 | The `next(iter(...))` rewrite is explicit and local. |
| **S8520** | Use "itertools.chain.from_iterable()" instead of "sum()" to flatten or concatenate lists. | "sum()" should not be used with an empty list to concatenate lists | ❓ unknown | 9/10 | The `sum(..., [])` to `itertools.chain.from_iterable()` rewrite is explicit and local. |
| **S8521** | Remove this unnecessary "keys()" call. | Dictionary membership tests should not explicitly call ".keys()" | ❓ unknown | 10/10 | Removing `.keys()` from a membership test is an exact local simplification. |
| **S8554** | Pass formatting arguments to the logging call instead of pre-formatting the message string. | Logging statements should follow Python best practices | ❓ unknown | 3/10 | A fix is imaginable, but it is not clearly safe or canonical enough for automation. |
| **S8572** | Use "logging.exception()" instead. | "logging.exception()" should be used instead of "logging.error()" in exception handlers | ❓ unknown | 10/10 | The logging API replacement is direct and local. |
| **S8685** | Use "field(default_factory=...)" instead of a function call as a default value. | Mutable or stateful values should not be used as default values in dataclass attributes | ❓ unknown | 9/10 | The `default_factory` rewrite is a known local transformation. |
| **BackticksUsage** | Use "repr" instead. | Backticks should not be used | ✅ exists | 10/10 | This is a direct local rewrite from backticks to `repr(...)`. |
| **ClassComplexity** | Class has a complexity of 4 which is greater than 2 authorized. | Cyclomatic Complexity of classes should not be too high | ❓ unknown | 0/10 | Lowering complexity is a refactor, not a deterministic rewrite. |
| **CommentRegularExpression** | Avoid TODO | Track comments matching a regular expression | ❓ unknown | 0/10 | The analyzer cannot know how the comment should be rewritten. |
| **ExecStatementUsage** | Do not use exec statement. | The "exec" statement should not be used | ❓ unknown | 0/10 | Replacing `exec` requires redesigning the code path. |
| **FileComplexity** | File has a complexity of 4 which is greater than 2 authorized. | Files should not be too complex | ❓ unknown | 0/10 | Lowering file complexity requires structural refactoring. |
| **FunctionComplexity** | Function has a complexity of 4 which is greater than 2 authorized. | Cyclomatic Complexity of functions should not be too high | ❓ unknown | 0/10 | Lowering function complexity requires structural refactoring. |
| **InequalityUsage** | Replace "<>" by "!=". | "<>" should not be used to test inequality | ❓ unknown | 10/10 | Exact token replacement from `<>` to `!=`. |
| **LineLength** | The line contains 40 characters which is greater than 30 authorized. | Lines should not be too long | ❓ unknown | 0/10 | Line wrapping has too many formatting and readability choices. |
| **LongIntegerWithLowercaseSuffixUsage** | Replace suffix in long integers from lower case "l" to upper case "L". | Long suffix "L" should be upper case | ❓ unknown | 10/10 | This is a direct one-token text replacement. |
| **NoSonar** | Is #NOSONAR used to exclude false-positive or to hide real quality flaw? | Track uses of "NOSONAR" comments | ❓ unknown | 0/10 | Removing `NOSONAR` is a review decision, not a safe code rewrite. |
| **OneStatementPerLine** | At most one statement is allowed per line, but 3 statements were found on this line. | Statements should be on separate lines | ❓ unknown | 8/10 | Splitting statements across lines is syntactic and locally derivable. |
| **ParsingError** | Parser exception message from the Python parser (dynamic). | Python parser failure | 🚫 infeasible | 0/10 | Parser failures have no reliable canonical automatic fix. |
| **PreIncrementDecrement** | This statement doesn't produce the expected result, replace use of non-existent pre-increment operator | Increment and decrement operators should not be used | ❓ unknown | 1/10 | The intended replacement is ambiguous; `++x` is not always `x += 1`. |
| **PrintStatementUsage** | Replace print statement by built-in function. | The "print" statement should not be used | ❓ unknown | 10/10 | This is a direct Python 2 statement-to-function rewrite. |
