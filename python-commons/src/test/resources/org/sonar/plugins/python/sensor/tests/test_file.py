# Lives under a `tests/` directory to trigger the directory-based heuristic.
# See sensor/test_file.py for the companion file used to test the filename-based heuristic.
def test_func(x):
    x = 42
    assert (a, b)
