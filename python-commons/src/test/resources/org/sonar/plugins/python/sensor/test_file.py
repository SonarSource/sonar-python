# Triggers the filename heuristic (test_*.py) but not the pytest-pattern heuristic.
# Used in tests that specifically exercise path-based test detection.
def regular_func(x):
    x = 42
    assert (a, b)
